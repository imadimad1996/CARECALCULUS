// Re-export the unified clinical CommandPalette to ensure a single source of truth across the entire app
export { default } from '../CommandPalette';
export type { CommandPaletteProps } from '../CommandPalette';
